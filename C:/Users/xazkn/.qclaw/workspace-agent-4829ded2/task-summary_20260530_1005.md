# Znote/DaniNote HTML列表方案实施 (2026-05-30 10:05)

## 目标
用 HTML+CSS 方案（方案4）重写列表逻辑，解决4个问题。

## 核心改动
- 列表创建：不再用 QTextListFormat/createList，改用 insertHtml 插入 `<ol>/<ul>` + CSS font-size
- 字号控制：通过 CSS `font-size` 属性设置列表字号，标记天然跟随
- 列表移除：收集列表项文本→删除列表区域→重新插入纯文本
- 字号增减：简化为 _apply_font_size()，有选区时 mergeCharFormat，无选区时 setCurrentCharFormat

## 文件
- F:\qclaw\Znote\src\views\note_editor.py — 完全重写列表和字号逻辑
- F:\qclaw\Znote\src\views\_test_list_font.py — 测试脚本（待清理）

## 待验证
- 用户手动测试5个场景确认修复效果
